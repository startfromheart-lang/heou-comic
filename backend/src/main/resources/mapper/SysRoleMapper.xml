<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heou.comicgenerator.mapper.SysRoleMapper">

    <resultMap type="SysRole" id="SysRoleResult">
        <id     property="id"          column="id"          />
        <result property="roleName"    column="role_name"   />
        <result property="roleCode"    column="role_code"   />
        <result property="description" column="description" />
        <result property="status"      column="status"      />
        <result property="createTime"  column="create_time" />
        <result property="updateTime"  column="update_time" />
    </resultMap>

    <sql id="selectRoleVo">
        select id, role_name, role_code, description, status, create_time, update_time from sys_role
    </sql>

    <select id="selectRoleList" parameterType="SysRole" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        <where>
            <if test="roleName != null and roleName != ''">
                AND role_name like concat('%', #{roleName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        order by id desc
    </select>

    <select id="selectRoleById" parameterType="Integer" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where id = #{id}
    </select>

    <select id="selectRoleByCode" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where role_code = #{roleCode}
    </select>

    <insert id="insertRole" parameterType="SysRole" useGeneratedKeys="true" keyProperty="id">
        insert into sys_role(role_name, role_code, description, status)
        values(#{roleName}, #{roleCode}, #{description}, #{status})
    </insert>

    <update id="updateRole" parameterType="SysRole">
        update sys_role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="roleCode != null and roleCode != ''">role_code = #{roleCode},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        where id = #{id}
    </update>

    <delete id="deleteRoleById" parameterType="Integer">
        delete from sys_role where id = #{id}
    </delete>

    <select id="countUserByRoleId" parameterType="Integer" resultType="Integer">
        select count(*) from sys_user_role where role_id = #{roleId}
    </select>

    <select id="selectMenuIdsByRoleId" parameterType="Integer" resultType="Integer">
        select menu_id from sys_role_menu where role_id = #{roleId}
    </select>

    <insert id="insertRoleMenu">
        insert into sys_role_menu(role_id, menu_id) values(#{roleId}, #{menuId})
    </insert>

    <delete id="deleteRoleMenuByRoleId" parameterType="Integer">
        delete from sys_role_menu where role_id = #{roleId}
    </delete>

    <delete id="deleteRoleMenuByMenuId" parameterType="Integer">
        delete from sys_role_menu where menu_id = #{menuId}
    </delete>
</mapper>